#
# Copy this file to .env.local (ignored by git) and adjust values as needed.
#

# Runtime config for SPA (must be prefixed with VITE_)
VITE_GHOST_CONTENT_URL=https://blog.goodmeow.my.id/ghost/api/content/posts/
VITE_GHOST_CONTENT_KEY=replace-with-content-api-key
