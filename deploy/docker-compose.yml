services:
  landing:
    image: nginx:alpine
    container_name: landing_www
    restart: unless-stopped
    volumes:
      - ../frontend/dist:/usr/share/nginx/html:ro
      - ./nginx/site.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      web:
        aliases:
          - landing
      default: {}
    expose:
      - "80"

networks:
  web:
    external: true
